# Dataset Sample

This project was trained on an Arabic speech dataset consisting of `.wav` audio files and `.lab` text transcriptions.

Due to dataset licensing, the dataset is not included.

You can reproduce the training using public datasets such as:
- Mozilla Common Voice (Arabic)
- OpenSLR Arabic Speech Corpus